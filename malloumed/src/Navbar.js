import React from 'react';
import './Navbar.css';

const Navbar = () => {
  return (
    <nav className="minimal-navbar">
      {/* No title, no links, keeping it empty for a minimalist approach */}
    </nav>
  );
};

export default Navbar;
